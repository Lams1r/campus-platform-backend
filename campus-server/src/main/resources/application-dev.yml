# 开发环境配置(连远程 MySQL / Redis)
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://8.148.245.72:3306/campus_system?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:ldx18933875010}
  data:
    redis:
      host: ${REDIS_HOST:8.148.245.72}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DATABASE:0}
      password: ${REDIS_PASSWORD:ldx18933875010}

mybatis-plus:
  configuration:
    # 开发环境打印 SQL 方便调试
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jetcache:
  remote:
    default:
      uri: ${JETCACHE_REDIS_URI:redis://ldx18933875010@8.148.245.72:6379/}

campus:
  cors:
    # 开发环境允许本地前端调试地址
    allowed-origins: http://localhost:5173,http://localhost:3000

logging:
  file:
    path: ./logs
  level:
    root: INFO
    com.campus: DEBUG
